# EquiLead — flat FastAPI/Jinja decision-support workspace. Python 3.11+
# Install everything with: python -m pip install -r requirements.txt

# ── Web ─────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.12
itsdangerous>=2.2
sqlalchemy>=2.0
pydantic>=2.10
python-dotenv>=1.0

# ── Models (required: train.py, scoring.py, explain.py) ─
numpy>=2.1
pandas>=2.2
scikit-learn>=1.5
joblib>=1.4

# ── Optional model extras ───────────────────────────────
# Gradient boosting uses XGBoost when installed and falls back to scikit-learn HistGradientBoosting otherwise.
# Tree explanations use SHAP when installed and fall back to reference-perturbation sensitivity otherwise.
xgboost>=2.1
shap>=0.46

# ── Tests ───────────────────────────────────────────────
pytest>=8.3
httpx>=0.28
playwright>=1.48        # optional: browser tests skip cleanly when not installed (python -m playwright install chromium)

# ── Optional secrets-at-rest support (security.py) ──────
# cryptography>=43
