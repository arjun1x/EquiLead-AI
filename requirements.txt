# EquiLead — flat FastAPI/Jinja project. Python 3.11+
# Demo mode needs only the "Web" block. Research mode (DEMO_MODE=0) also needs the "ML" block.

# ── Web ─────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.12
itsdangerous>=2.2
sqlalchemy>=2.0
pydantic>=2.10
python-dotenv>=1.0

# ── Tests ───────────────────────────────────────────────
pytest>=8.3
httpx>=0.28
playwright>=1.48        # optional: browser tests skip cleanly when not installed

# ── ML (research mode only) ─────────────────────────────
# xgboost>=2.1
# shap>=0.46
# pandas>=2.2
# numpy>=2.1
# scikit-learn>=1.5
# anthropic>=0.40

# ── Optional secrets-at-rest support ────────────────────
# cryptography>=43
